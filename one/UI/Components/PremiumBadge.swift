//
//  PremiumBadge.swift
//  one
//
//  Premium sistemi devre dışı — bu component artık kullanılmıyor.
//

import SwiftUI

/// Stub — premium kaldırıldığı için kullanılmıyor.
struct PremiumBadge: View {
    var body: some View { EmptyView() }
}

/// Stub — premium kaldırıldığı için kullanılmıyor.
struct PremiumLockOverlay: View {
    var body: some View { EmptyView() }
}
